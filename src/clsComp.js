import React from 'react';
export const presentationScreen = (props) => (
    <div>
        <p>{props.name}</p>
    </div>
)



export const Button = ( props) => {
    <p></p>
    <input >
    <button>Print</button>
}