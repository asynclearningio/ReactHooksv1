import React from 'react';
export const presentationScreen = (props) => (
    <div>
        <p>{props.name}</p>
    </div>
)



export const Button = (props) => {
    return (
        <div>
            <input />
            <button>Print</button>
        </div>

    )
}